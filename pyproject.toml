[project]
name = "pythontts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "requests",
    "tenacity",
]
[project.optional-dependencies]
dev = [
    "httpx",
]